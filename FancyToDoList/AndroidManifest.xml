<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.aripio.wishlist"
      android:versionCode="1"
      android:versionName="1.0"
      android:minSdkVersion="4" >


    <application android:label="@string/app_name" android:debuggable="true" android:icon="@drawable/checkicon">
    <uses-library android:name="com.google.android.maps" />    
         <activity android:name=".WishListLogin"
                  android:label="@string/app_name"
                  android:theme="@style/ToDoTheme"
                  android:screenOrientation="portrait">
<!--             <intent-filter> -->
<!--                 <action android:name="android.intent.action.MAIN" /> -->
<!--                 <category android:name="android.intent.category.LAUNCHER" /> -->
<!--             </intent-filter> -->
            
        </activity>    
        
	    <activity android:name=".WishListLoginReal">
	    </activity>
	    
	    <!-- The default activity of the app.  Can also display search results. -->
        <activity android:name=".activity.WishList"
                  android:label="@string/app_name"
                  android:theme="@style/ToDoTheme"
                  android:screenOrientation="portrait">
<!--                   android:launchMode="singleTask"> -->
                  
<!--                   <meta-data android:name="android.app.default_searchable" -->
<!--                    	   android:value=".WishListSearchEngine" /> -->
                   	   
                <intent-filter>
                	<action android:name="android.intent.action.MAIN" />
                	<category android:name="android.intent.category.LAUNCHER" />
            	</intent-filter>
            	
	            <!-- Receives the search request. -->
	            <intent-filter>
	                <action android:name="android.intent.action.SEARCH" />
	                <category android:name="android.intent.category.DEFAULT" />
	            </intent-filter>
	            
<!-- 	            Points to searchable meta data. -->
            	<meta-data android:name="android.app.searchable"
                    android:resource="@xml/searchable"/>
            
        </activity>
        
        <activity android:name=".activity.EditItemInfo"
        		  android:label="@string/app_name" 
                  android:theme="@style/ToDoTheme"
                  android:screenOrientation="portrait">
        </activity>
        
        <activity android:name="WishListMap"
        		  android:label="@string/app_name" 
                  android:theme="@style/ToDoTheme">
        </activity> 
        
        <activity android:name=".activity.WishItemDetail"
        		  android:label="@string/app_name" 
                  android:theme="@style/ToDoTheme">
                  android:screenOrientation="portrait">
        </activity> 
        
         <activity android:name=".activity.WishItemPostToSNS"
        		  android:label="@string/app_name" 
                  android:theme="@style/ToDoTheme">
        </activity> 
        
<!--         <activity android:name=".WishListSearchEngine" -->
<!--                 android:label="@string/app_name" -->
<!--                 android:theme="@style/ToDoTheme"> -->
<!--             <intent-filter> -->
<!--                 <action android:name="android.intent.action.SEARCH" /> -->
<!--             </intent-filter> -->
<!--               <meta-data android:name="android.app.searchable" -->
<!--                     android:resource="@xml/searchable"/> -->
<!--         </activity> -->
        
        <activity android:name=".activity.WishListNewMap"
        android:label="@string/app_name" 
                  android:theme="@style/ToDoTheme"></activity>
                  
<!--         <provider android:name="com.aripio.wishlist.defaultphotoprovider" -->
<!--           android:authorities="com.aripio.wishlist.provider.defaultphotoprovider"> -->
<!-- 		</provider> -->
		
<!-- 		<provider android:name="com.google.provider.NotePad" -->
<!--           android:authorities="com.google.provider.NotePad"> -->
<!-- 		</provider> -->
		
             
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />  
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> 
</manifest>